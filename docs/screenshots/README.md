# Screenshots

Save the website screenshots in this folder with these exact filenames so the main README displays them:

| Filename | What it should show |
|----------|---------------------|
| `home-hero.png` | The home page hero (name + tagline over the photo) |
| `home-profile.png` | The "A builder of institutions…" profile teaser |
| `footer.png` | The gold contact band + navy footer with credits |

Tip: take full-width browser screenshots (PNG) and drop them here. The names must match exactly (lowercase, hyphens).
